# ambiente
.env
.venv/

# dados gerados (exceto prompts do agente)
data/raw/
data/chunks/
data/parsed/

# jupyter
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# python
__pycache__/
*.pyc
*.pyo

# sistema
.DS_Store
Thumbs.db
.vercel

# avaliacao (eval): config/cache do deepeval e traces brutos
.deepeval/
.deepeval-cache.json
.deepeval_telemetry.txt
# eval/runs: versionado como prova (relatorios + tarefas/vereditos do juiz).
# So o coleta.jsonl (traces brutos, ~10MB, regenerado por inteiro a cada run) fica de fora, para nao inflar o historico.
eval/runs/coleta.jsonl
