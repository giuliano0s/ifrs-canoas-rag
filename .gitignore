# ambiente
.env
.venv/

# dados gerados (exceto prompts do agente)
data/raw/
data/chunks/
data/parsed/

# jupyter
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# python
__pycache__/
*.pyc
*.pyo

# sistema
.DS_Store
Thumbs.db
.vercel

# avaliacao (eval): config/cache do deepeval e traces brutos
.deepeval/
.deepeval-cache.json
.deepeval_telemetry.txt
# eval/runs: versiona a prova leve (relatorios + juiz_vereditos). Os pesados/derivados
# (coleta bruta ~10MB e juiz_tarefas ~5MB, regenerados a cada run) ficam fora, para nao inflar o historico.
eval/runs/coleta.jsonl
eval/runs/juiz_tarefas.jsonl
